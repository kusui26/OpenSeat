/**
 * 画面が出す幅。
 *
 * **ここは「ボタンでどこまで選べるか」であって、施設の決まりではない。**
 * 本当の上限は運用パラメータ（`maxPartySize`。7.16）で、判断するのは `core`
 * である（CLAUDE.md 3.1）。**画面が先回りして断らない。**
 *
 * これを超える組には、分けて登録してもらう（7.5）。いまは施設の設定を画面へ
 * 渡していないので、**受付でも人数の変更でも同じ幅**にしてある —— 片方だけ
 * 広いと、変えようとして初めて断られることになる。
 */
export const STEPPER_MAX_PARTY_SIZE = 10;

/** 1 名より少ない組はいない。 */
export const STEPPER_MIN_PARTY_SIZE = 1;
