# OpenSeat Phase 1 実装プラン — コアとシミュレーション

- 作成日: 2026-09-17
- 対象: 開発プラン [`260916_plan_OpenSeat.md`](260916_plan_OpenSeat.md) の 11.3「Phase 1（コアとシミュレーション）の作業」を、実装可能な単位まで分解したもの
- 位置づけ: **全体プランが「何を作るか」、本書が「どの順番で、どの粒度で作るか」を定める。** 設計の出典は常に全体プランで、本書と食い違ったら全体プランを正とする
- 実装時の規約は [`.claude/CLAUDE.md`](../.claude/CLAUDE.md)。特に第 3 章（アーキテクチャの鉄則）は本書のすべての PR に適用される
- 各フェーズのプランは `YYMMDD_plan_PhaseN.md` として作る。Phase 2 以降も同じ形式にする

## 目次

1. Phase 1 のゴールと完了条件
2. 現在地
3. PR の粒度と順番をどう決めたか
4. PR 一覧と依存関係
5. 各 PR の詳細
6. すべての PR に適用する横断ルール
7. 設計上の先決事項
8. スケジュール
9. Phase 1 で決めること（ADR）
10. リスクと対策
11. Phase 2 への引き継ぎ
12. 付録

---

## 1. Phase 1 のゴールと完了条件

### ゴール

**7 章のコアロジック仕様を、依存ゼロの純粋関数として実装し、シミュレーションで既定値を数字で裏づける。**

Phase 1 が終わった時点で、サーバも画面も存在しない。あるのは `packages/core` と、それを大量に叩くシミュレータ、そして「なぜこの既定値なのか」を説明できる比較レポートである。

### 完了条件

全体プラン 11.1 は「不変条件テストが緑、レポートで既定値を決定」としている。これを検査可能な形に落とす。

| # | 条件 | 検査方法 |
|---|---|---|
| C1 | 7.3 と 7.4 の状態機械が、宣言された遷移表として実装されている | 遷移表の全行にテストがあり、表にない遷移が拒否されること |
| C2 | 9.12 の 5 つの不変条件が宣言され、すべてのコマンド適用後に検査される | 不変条件ごとに「成立する例」と「破れる例」のテスト |
| C3 | 7.6 の割当アルゴリズムが、数値例とエッジケース表を含めて実装されている | 7.6 の 2 つの数値例と 9 行のエッジケース表が 1 行 1 テストで写されていること |
| C4 | 7.8 の座席 QR の 10 分岐がすべて実装されている | 表の 10 行それぞれにテスト |
| C5 | `tick` が冪等で、時間が大きく飛んでも状態が壊れない | 性質テスト（同一時刻での反復、ランダムな時間跳躍） |
| C6 | シミュレータが 8.1 のモデルを再現し、同じシードで同じ結果になる | 決定性のテスト、分布の統計的なテスト |
| C7 | 8.2 の 7 項目の比較が実行でき、8.3 の指標が出る | CLI が動き、CSV が出ること |
| C8 | 200 回のシミュレーション実行で不変条件違反がゼロ | CI でのファズ実行 |
| C9 | 既定値が数字で裏づけられ、ADR に記録されている | ADR-0007〜0010 が存在し、レポートを引いていること |
| C10 | 品質ゲートがすべて緑 | `pnpm verify` |

### Phase 1 でやらないこと

| やらないこと | いつやるか |
|---|---|
| 永続化（SQLite、Drizzle、イベントログ） | Phase 2 |
| HTTP API、WebSocket、VenueActor | Phase 2 |
| 認証と権限表（CLAUDE.md 3.2(4)） | Phase 2。権限はサーバの実行者概念に依存する |
| 通知（画面内・Web Push・LINE） | Phase 2 |
| 画面（利用者・ボード・スタッフ・管理） | Phase 2 |
| フロア図・席の座標 | Phase 3。割当は座標を使わない |
| 連結チケット（5 名以上の分割）、ゾーン希望 | v2（全体プラン 11.7） |
| ETA の統計モデル | v1.1。Phase 1 は 7.13 の v1 アルゴリズムまで |

---

## 2. 現在地

Phase 0 で次が用意されている（PR #1、コミット `cba25b3`）。

**足場**: pnpm ワークスペース、TypeScript 5.9（strict、`noUncheckedIndexedAccess`、`exactOptionalPropertyTypes`）、ESLint 9、Vitest 3、GitHub Actions の CI。

**境界の強制**: `scripts/check-architecture.mjs` が、`packages/core` の依存ゼロと純粋性（`Date.now` / `Math.random` / `process` / `node:` の import / `fetch` / `console` の不在）を CI で検査する。ESLint にも同じ制約がある。

**`packages/core` に既にあるもの**

| ファイル | 内容 |
|---|---|
| `time.ts` | `Timestamp`（UTC ミリ秒）、`DurationMs`、`minutes` / `seconds` / `after` / `hasPassed` / `remaining` / `elapsedSince` |
| `decision.ts` | `Decision<State, Event>`、`Apply` と `Tick` の型、`unchanged` / `decided` / `sequence` |
| `invariant.ts` | `Invariant` / `TransitionInvariant` の宣言と検査、`InvariantError` |

**まだ無いもの**: ドメインの型、状態機械、割当、ETA、シミュレータ。Phase 1 はここを埋める。

**Phase 1 で追加が必要な依存**: `fast-check`（性質テスト）のみ。`packages/core` 本体の依存ゼロは維持し、`devDependencies` に入れる。シミュレータは `packages/core/sim` に置き、こちらも依存ゼロで書く（決定的な乱数は自前で実装する。理由は 7 章）。
---

## 3. PR の粒度と順番をどう決めたか

ここが本書の中心である。分解の結果だけでなく、なぜその分解になったかを残す。

### 3.1 粒度の原則

**原則 1: 1 つの PR は、レビュアーが答えられる 1 つの問いに対応する。**

「型は 7.2〜7.4 と 7.16 を過不足なく表しているか」「遷移表は図と一致しているか」「割当は 7.6 のとおりか」。問いが 2 つ以上あるなら分ける。問いが立てられないなら、その PR は目的が曖昧である。各 PR の詳細（5 章）は必ず「問い」から始める。

**原則 2: 実装は 300〜500 行を目安にする。テストは別枠で数える。**

目安はコメントと空行を除いた**実装の行数**に適用する。テストを含めると、仕様の表を 1 行 1 テストで写す方針（原則 5、6.3）のぶんだけ膨らみ、目安が意味を失うためである。

実測（PR 1）: 実装 434 行、テスト 665 行、合計 1,099 行。実装は目安どおりだが、テストが 1.5 倍あった。以降の PR も同じ比率になると見込む。

実装が 800 行を超えたら分割を検討する。レビューの質は行数に反比例し、自分でレビューする場合も同じである。逆に 100 行を下回るなら、前後の PR に吸収できないかを考える。**テストの行数が多いこと自体は分割の理由にしない。** 1 行 1 アサーションのテストは平坦に読めるため、複雑なロジックと同じようには効いてこない。

**原則 3: `main` は常に緑で、意味のある状態に保つ。**

途中の PR でも、実装されている範囲では不変条件が成り立ち、テストが通る。「次の PR で直す」前提のコードを入れない。未実装のコマンドは型に存在させない（判別可能なユニオンに足さない）ことで、網羅性チェックを正直に保つ。

**原則 4: 後で捨てる実装を書かない。**

仮実装やスタブを置かない。特に割当アルゴリズムを「とりあえず FIFO」で入れて後から差し替える、という進め方は採らない。テストごと書き直すことになり、二度手間になる。

**原則 5: 宣言 → 振る舞い → 検証 の順に進む。**

CLAUDE.md 3.2 の思想をそのまま日程に落とす。宣言（型・遷移表・不変条件・ポリシー）は 7 章の仕様をコードへ転記する作業であり、レビューは「計画書と一致しているか」の照合になる。振る舞いはその宣言に照らして検証できる。逆順にすると、振る舞いを見ながら宣言を書くことになり、宣言が実装の追認になってしまう。

**原則 6: 検証の道具を早く立ち上げ、以降の PR を自動で叩かせる。**

これが後述する「シミュレータを中盤に置く」判断の根拠である。

### 3.2 順番を決めた 3 つの判断

分解そのものより、順番のほうが結果を左右する。3 つの判断を記録しておく。

#### 判断 1: 宣言（型・遷移表・不変条件）を最初に固める

**代替案**: 歩くスケルトン（受付から退席までの最小の一周）を最初に作り、そこから枝を伸ばす。

**採らなかった理由**: この Phase には I/O が無い。関数呼び出しで組み合わさるだけなので、早期に発見すべき「結合の問題」が存在しない。スケルトンが見つけてくれるものが無い。一方で、宣言を先に置くと、以降のすべての PR が「宣言に照らして正しいか」で検証できるようになる。得るもののほうが大きい。

#### 判断 2: 割当アルゴリズムは 4 番目。最初ではない

**代替案**: 最も難しいロジックなので、型の直後に置いて、設計の誤りを早く見つける。

**検討した結果**: **Phase 1 の本当のリスクは「割当が実装できるか」ではない。** 7.6 には擬似コードと数値例があり、実装は機械的である。本当のリスクは **「既定値が正しいかどうかを、数字で言えるようになるか」** で、これを解消するのはシミュレータであってアルゴリズムの実装ではない。

したがって割当は、依存（型）が揃った直後で、かつ適用（呼び出し）の直前に置く。純粋な選択関数として単独で実装し、状態への反映とは分ける（PR 4 と PR 6 の分離）。これにより 7.6 のエッジケース表 9 行を、状態遷移の都合に邪魔されずにテストできる。

#### 判断 3: シミュレータを最後ではなく中盤（8 番目）に置く ← 本書で最も重要な判断

**当初の案**: シミュレータは検証の道具なので、コアが完成してから作る（13〜15 番目）。

**採らなかった理由が 3 つある。**

1. **シミュレータはファズの実行環境そのものである。** 全体プラン 9.12 が「シミュレータをファズテストとして流し、不変条件違反を検出する」と定めている。最後に置くと、それまでの 7 つの PR は手書きのテストだけで守られることになる。中盤に置けば、以降のすべての PR がランダムなコマンド列で自動的に叩かれる。
2. **Phase 1 の成果物の価値はレポートにある。** 日程が押したときに削られるのは末尾の PR である。シミュレータが末尾にあると、Phase 1 の目的そのものが失われる。中盤にあれば、最悪でも「動くシミュレータと粗いレポート」が残る。
3. **早く数字が出ることの効用。** 割当方式の比較は、コアが 100% 完成していなくても粗い答えが出る。方向が違っていれば早く分かる。

**置ける最も早い位置は、基本の循環が閉じた直後である。** 受付 → 割当 → 呼び出し → 着席 → 退席 → 再割当 が通れば、シミュレータは回せる。それが PR 7 の完了時点なので、PR 8 に置く。

**懸念と対処**: 以降の PR（飛び込み、ノーショー、無断利用）でシミュレータのモデルを拡張する必要がある。ただしシミュレータの骨格（イベントループ、決定的な乱数、分布）は変わらず、増えるのは「どの確率的な判断を注入するか」だけなので、手戻りは小さい。指標の収集と CLI は PR 14 に分けて、骨格を軽く保つ。

### 3.3 分解の副産物として得られること

- **停止しやすい。** どの PR の直後でも中断でき、次に再開しやすい。
- **レビューの問いが計画書に紐づく。** すべての PR が 7 章か 8 章の特定の節に対応する。実装が仕様から離れにくい。
- **`main` の履歴が仕様の実装順になる。** 後から「なぜこの順で作ったか」を辿れる。

---

## 4. PR 一覧と依存関係

15 個の PR を 4 つの段に分ける。

| # | PR | 段 | 出典 | 見積 | 依存 |
|---|---|---|---|---|---|
| 1 | ドメインの型とポリシー | A 宣言 | 7.2〜7.4、7.16 | 4h | — |
| 2 | 状態機械の遷移表 | A 宣言 | 7.3、7.4 | 4h | 1 |
| 3 | 不変条件の宣言 | A 宣言 | 9.12 | 3h | 1 |
| 4 | 割当の選択 | B 循環 | 7.6 | 6h | 1 |
| 5 | `apply` の骨格と受付・キャンセル・保留 | B 循環 | 7.5、7.9 | 6h | 2, 3 |
| 6 | 呼び出し・ホールド・ノーショー | B 循環 | 7.7 | 6h | 4, 5 |
| 7 | 着席と退席 | B 循環 | 7.8 の正常系、7.11 の 1 層目 | 4h | 6 |
| 8 | **シミュレータの骨格** | B 循環 | 8.1 | 7h | 7 |
| 9 | 座席 QR の全分岐と飛び込み | C 枝葉 | 7.8 の残り、7.12 | 6h | 7 |
| 10 | 時間上限と整合性回復 | C 枝葉 | 7.10、7.11 | 6h | 7 |
| 11 | 運用時間帯とモード切替 | C 枝葉 | 7.14 | 4h | 5 |
| 12 | `tick` の性質と遷移表の網羅 | C 枝葉 | 9.4、9.12 | 5h | 6, 9, 10, 11 |
| 13 | 待ち時間の推定（ETA） | D 測る | 7.13 | 4h | 7 |
| 14 | 指標の収集と CLI | D 測る | 8.3、8.4 | 6h | 8, 12, 13 |
| 15 | 方針比較レポートと ADR | D 測る | 8.2 | 6h | 14 |

合計 **77 時間**。全体プラン 11.6 の Phase 1 見積もり（60〜80h）の上寄りに収まる。

### 依存関係

```
                   ┌──────────────────────┐
                   │ 1. 型とポリシー         │
                   └──┬─────────┬────────┬─┘
                      │         │        │
          ┌───────────▼──┐  ┌───▼──────┐ │
          │ 2. 遷移表      │  │ 3. 不変条件 │ │
          └───────────┬──┘  └───┬──────┘ │
                      │         │        │
                      └────┬────┘   ┌────▼──────────┐
                           │        │ 4. 割当の選択    │
                  ┌────────▼─────┐  └────┬──────────┘
                  │ 5. apply 骨格  │       │
                  │  受付・キャンセル │◀──────┘
                  └───┬───────┬──┘
                      │       │
          ┌───────────▼──┐    │      ┌──────────────────┐
          │ 6. 呼び出し     │    └─────▶│ 11. 運用時間帯     │
          │  ホールド       │           └────────┬─────────┘
          └───────────┬──┘                    │
                      │                        │
          ┌───────────▼──┐                     │
          │ 7. 着席と退席   │                     │
          └─┬──────┬───┬─┘                     │
            │      │   │                        │
  ┌─────────▼─┐ ┌──▼───────────┐ ┌────────────▼──┐
  │ 8. シミュ骨格│ │ 9. 座席QR全分岐 │ │ 10. 時間上限     │
  └─────────┬─┘ └──┬───────────┘ └────────────┬──┘
            │      │                           │
            │      └───────────┬───────────────┘
            │                  │
            │        ┌─────────▼──────────────┐
            │        │ 12. tick の性質・網羅     │
            │        └─────────┬──────────────┘
            │                  │
  ┌─────────▼──────┐           │      ┌──────────────┐
  │ 13. ETA         │           │      │              │
  └─────────┬──────┘           │      │              │
            └────────┬─────────┘      │              │
                     │                │              │
           ┌─────────▼────────────────▼──┐           │
           │ 14. 指標と CLI                │           │
           └─────────┬────────────────────┘           │
                     │                                │
           ┌─────────▼────────────────────┐           │
           │ 15. 方針比較レポートと ADR       │◀──────────┘
           └──────────────────────────────┘
```

### 並行できるもの

一人開発なので基本は直列だが、気分を変えたいときに入れ替えられる組み合わせを挙げておく。

- **2 と 3 と 4** は互いに独立（すべて 1 だけに依存）。順不同でよい。
- **9 と 10 と 11** は互いに独立。11 は 5 の直後でも作れる。
- **13（ETA）** は 7 の後ならいつでもよい。行き詰まったときの気分転換に向く。

### 最短の打ち切り線

日程が押した場合にどこで止めれば Phase 1 の価値が残るか。

| 止める位置 | 残るもの | 失うもの |
|---|---|---|
| PR 8 まで | 基本の循環と、それを叩くシミュレータ。Phase 2 の着手は可能 | 異常系、上限、ETA、比較レポート |
| PR 12 まで | コアロジックが仕様どおり完成。ファズで守られている | 数字の裏づけ。既定値は 7.16 の暫定値のまま |
| **PR 15 まで** | **完了条件をすべて満たす** | — |

**PR 12 で止めるのは避けたい。** 既定値を数字で裏づけることが Phase 1 の目的なので、そこを落とすと実証実験の設定に根拠がなくなる。押した場合は 9・10・11 のいずれかを Phase 2 へ送るほうがよい（これらは実証実験の当日までに間に合えばよい）。
---

## 5. 各 PR の詳細

各 PR は「問い」から始める。レビュー（自分で読み返す場合も含む）は、この問いに答えられるかだけを見る。

### 段 A — 宣言（仕様をコードにする）

---

#### PR 1 — ドメインの型とポリシー

> **問い: 型は 7.2〜7.4 の用語・状態と、7.16 のパラメータを、過不足なく表しているか。**

**出典**: 全体プラン 7.2（用語）、7.3（チケットの状態）、7.4（テーブルの状態）、7.16（パラメータ一覧）、9.6（データモデル）
**依存**: なし ／ **見積**: 4h ／ **目安**: 実装 400 行 ／ **実測**: 実装 434 行・テスト 665 行（189 テスト）

**実装**

`packages/core/src/domain/` に置く。

| ファイル | 内容 |
|---|---|
| `ids.ts` | `VenueId` / `TableId` / `TicketId` / `TicketCode`。いずれも `string` の別名。`as` を使わずに済むよう、ブランド型は使わない |
| `table.ts` | `TableStatus`（`DISABLED` / `FREE` / `HELD` / `OCCUPIED` / `OCCUPIED_UNKNOWN` / `TURNOVER` / `NEEDS_CHECK` の 7 値）と `Table` |
| `ticket.ts` | `TicketState`（`WAITING` / `PAUSED` / `CALLED` / `SEATED` / `DONE` / `CANCELLED` / `NO_SHOW` / `EXPIRED` の 8 値）、`EndReason`、`Ticket` |
| `state.ts` | `VenueState`（tables、tickets、policy、運用状態、表示コードの採番） |
| `policy.ts` | `Policy` と `DEFAULT_POLICY`。7.16 の全項目 |

型の骨格（詳細は実装時に詰める）:

```ts
export interface Table {
  readonly id: TableId;
  readonly label: string;              // 印刷される席番号
  readonly capacity: number;
  readonly tags: readonly string[];    // 車いす対応・子ども椅子など
  readonly adminRank: number;          // 管理者が決めた処理順
  readonly enabled: boolean;           // 対象席か
  readonly status: TableStatus;
  readonly statusSince: Timestamp;
  readonly verifiedFreeAt: Timestamp | null;  // 退席が確認された時刻。7.6 の並び順に使う
  readonly occupantTicketId: TicketId | null; // HELD / OCCUPIED のとき
  readonly disableAfterCurrent: boolean;      // 7.6 のエッジケース
}

export interface Ticket {
  readonly id: TicketId;
  readonly code: TicketCode;           // A-23
  readonly partySize: number;
  readonly requiredTags: readonly string[];
  readonly state: TicketState;
  readonly priorityAt: Timestamp;      // 順番の基準。人数を増やすと現在時刻に更新
  readonly createdAt: Timestamp;       // 絶対上限（ticket_max_age_min）の起点
  readonly tableId: TableId | null;
  readonly calledAt: Timestamp | null;
  readonly holdDeadline: Timestamp | null;
  readonly extensions: number;
  readonly passes: number;
  readonly noShows: number;
  readonly conflictPriority: boolean;  // 7.8「席が塞がっていた」で立つ
  readonly seatedAt: Timestamp | null;
  readonly endedAt: Timestamp | null;
  readonly endReason: EndReason | null;
  readonly pauseDeadline: Timestamp | null;
  readonly pausedTotal: DurationMs;
  readonly lastSeenAt: Timestamp;      // 接続のハートビート。7.9 の放置判定
  readonly hasNotificationChannel: boolean;  // 通知手段の有無。放置判定を分ける
  readonly stillHereAskedAt: Timestamp | null;
}
```

**この PR で決めること**

- **状態は配列で持つ。** `readonly Table[]` と `readonly Ticket[]`。1 施設あたり席 20〜100、有効チケット 200 程度なので、線形走査で十分（全体プラン 9.1）。`Map` にすると更新のたびの再構築が読みにくくなる。引き当てはヘルパー関数に閉じ込め、後から差し替えられるようにする。
- **ID は core で生成しない。** 生成には乱数が要り、core は乱数を持てない。ID はコマンドに含めて外から渡す。
- **表示コード（`A-23`）は core で採番する。** 状態から決定的に導けるため（`nextCodeSeq`）。

**テスト**

- `DEFAULT_POLICY` のキー集合が、7.16 の表の項目と 1 対 1 で一致すること。表の項目名を明示的な配列としてテストに書き、差分を検出する
- 各既定値が 7.16 と一致すること（1 行 1 テスト）
- `Policy` の整合性検証: `hold_reminder_before_min < hold_min`、`time_limit_min > 0`、`max_extensions >= 0`、`pause_max_total_min >= pause_step_min` など
- `TicketState` が 8 値、`TableStatus` が 7 値であること（状態の追加漏れを検出する）

**完了条件**: 型だけで `pnpm verify` が緑。`DEFAULT_POLICY` が 7.16 の表と一致することがテストで保証される。

**実装後の記録（2026-09-17）**: 7.16 の表は 25 行で、うち 3 行が 2 つのキーを定義するため **キーは 28 個**だった。本書の当初の記述（「全項目」）は行数と混同しやすかったため、テストでは行名とキー名の対応表を持ち、両方向の網羅を検査する形にした。`still_here_prompt_at` → `stillHerePromptMin`、`join_rate_limit` → `joinRateLimitPerHour`、`eta_display` → `etaBucketMin` の 3 つは名前を変えたので、対応表に理由を残してある。

---

#### PR 2 — 状態機械の遷移表

> **問い: 遷移表は 7.3 と 7.4 の図と一致しているか。抜け、到達不能、行き止まりはないか。**

**出典**: 全体プラン 7.3、7.4 ／ CLAUDE.md 3.2(2)
**依存**: PR 1 ／ **見積**: 4h ／ **目安**: 実装 350 行 ／ **実測**: 実装 620 行・テスト 581 行（220 テスト）

**実装**

`packages/core/src/machine/transitions.ts`。7.3 と 7.4 の状態遷移図を、コードの分岐ではなく **データの表** として宣言する。

```ts
export const TICKET_TRANSITIONS = [
  { from: 'WAITING', on: 'CALL',        to: 'CALLED',    guard: 'fitsCapacity' },
  { from: 'WAITING', on: 'PAUSE',       to: 'PAUSED',    guard: null },
  { from: 'WAITING', on: 'CANCEL',      to: 'CANCELLED', guard: null },
  { from: 'WAITING', on: 'ABANDON',     to: 'EXPIRED',   guard: 'noNotificationChannel' },
  { from: 'PAUSED',  on: 'READY',       to: 'WAITING',   guard: null },
  { from: 'CALLED',  on: 'CHECK_IN',    to: 'SEATED',    guard: 'isAssignedTable' },
  { from: 'CALLED',  on: 'EXTEND',      to: 'CALLED',    guard: 'underExtensionLimit' },
  { from: 'CALLED',  on: 'PASS',        to: 'PAUSED',    guard: null },
  { from: 'CALLED',  on: 'HOLD_EXPIRE', to: 'PAUSED',    guard: 'firstNoShowAndRequeueOnce' },
  { from: 'CALLED',  on: 'HOLD_EXPIRE', to: 'NO_SHOW',   guard: 'finalNoShow' },
  { from: 'CALLED',  on: 'REPORT_TAKEN',to: 'WAITING',   guard: null },
  { from: 'SEATED',  on: 'CHECK_OUT',   to: 'DONE',      guard: null },
  // ... 7.3 の図の全矢印
] as const satisfies readonly TicketTransition[];
```

同じ形で `TABLE_TRANSITIONS` を宣言する。

あわせて `findTransition(from, on)` と、表にない遷移を拒否する `transit()` を実装する。**ガードの中身はこの PR では実装しない。** 名前だけを列挙し、実装は各振る舞いの PR で埋める。ガード名の型が実装済みのガードの集合と一致することは PR 12 で閉じる。

**テスト**

- 表の全行について、`transit` が宣言どおりの遷移を返すこと
- 表にない `(from, on)` の組み合わせが拒否されること（無作為な組み合わせで網羅）
- **終端状態（`DONE` / `CANCELLED` / `NO_SHOW` / `EXPIRED`）から出る遷移が無いこと**
- **すべての状態が初期状態から到達可能であること**（グラフの到達可能性）
- **すべての非終端状態から、いずれかの終端状態へ到達できること**（行き止まりが無いこと）
- 同じ `(from, on)` に複数の遷移がある場合（`HOLD_EXPIRE` のように）、ガードが互いに排他であることを宣言で確認できること

到達可能性と行き止まりの検査は、図を目で追うより確実で、状態を足したときに自動で効く。

**完了条件**: 7.3 と 7.4 の図のすべての矢印が表に存在し、グラフの性質テストが緑。

**実装後の記録（2026-09-17）**

**図に無い遷移が 8 つ見つかった。** 7.3 と 7.4 の図は 7.6〜7.14 の本文より先に書かれており、本文で決めた振る舞いが図に反映されていなかった。CLAUDE.md 6.4 に従い、実装の前に全体プランの図を直した。

| # | 見つかった遷移 | 出典 | 図に無かった理由 |
|---|---|---|---|
| 1 | `WAITING → SEATED` | 7.8 の表 4 行目 | 待っている人が空席の QR を読んで前倒し着席する経路 |
| 2 | `CALLED → CALLED`（席の変更） | 7.8 の表 2 行目 | 図の `CALLED → CALLED` は延長だけを指していた |
| 3 | `CALLED → WAITING`（末尾へ） | 7.7 の 6 | `requeue_back` の行き先が図に無かった |
| 4 | `WAITING/PAUSED → EXPIRED`（絶対上限） | 7.7 の 7 | 図のラベルは放置と保留超過だけだった |
| 5 | 施設都合のキャンセル | 7.9 | 図のラベルは「本人キャンセル／スタッフ」だけだった |
| 6 | `TURNOVER → DISABLED` | 7.6 のエッジケース | `disable_after_current` の行き先が図に無かった |
| 7 | `NEEDS_CHECK/TURNOVER → DISABLED`（運用終了） | 7.14 | 図は `FREE → DISABLED` だけだった |
| 8 | `OCCUPIED_UNKNOWN → FREE` | 7.11 の 4 層目 | スタッフが「空席にする」経路が図に無かった |

あわせて、全席解放（緊急）ですべての席が `DISABLED` になることも図に加え、**運用終了と全席解放が別の操作である**ことを本文に明記した。

**この PR で決めたこと**

- **`CALLED` に受付からの絶対上限（`MAX_AGE`）を適用しない。** 絶対上限は幽霊チケットを防ぐためのもので、呼び出しに至っていない人が対象。席を確保した人を上限で打ち切ると、席を無駄にしたうえで利用者にも不親切になる。呼び出し後はホールドの期限が働くので幽霊にはならない。`MAX_AGE_APPLIES_TO` として宣言し、表と一致することをテストで守る。
- **運用終了（`CLOSE`）で即座に外れるのは `FREE` / `TURNOVER` / `NEEDS_CHECK` だけ。** 利用中の席は `disable_after_current` を立てて、現在の利用が終わってから外す。`CLOSE_APPLIES_TO` として宣言。

**規模**: 実装 620 行は目安の 350 行を超えた。遷移 53 本（チケット 25、テーブル 28）それぞれに出典の節番号と日本語の説明を持たせたためで、これは表を仕様として読めるようにするための費用である。

---

#### PR 3 — 不変条件の宣言

> **問い: 不変条件は「製品が成立するための条件」を過不足なく捉えているか。**

**出典**: 全体プラン 9.12 ／ CLAUDE.md 3.2(3) ／ 既存の `invariant.ts`
**依存**: PR 1 ／ **見積**: 3h ／ **目安**: 実装 300 行 ／ **実測**: 実装 241 行・テスト 482 行（66 テスト）

**実装**

`packages/core/src/machine/invariants.ts`。9.12 の 5 つに、型では表せない整合性を加える。

| 名前 | 内容 | 出典 |
|---|---|---|
| `one_ticket_per_table` | 1 つのテーブルに、`CALLED` または `SEATED` のチケットは最大 1 枚 | 9.12 (1) |
| `assigned_has_table` | `CALLED` / `SEATED` のチケットは必ず 1 つのテーブルを持つ | 9.12 (2) |
| `no_starvation` | 割当の実行後、「収まる空席があるのに `WAITING` の対象者が残る」ことはない | 9.12 (3) |
| `priority_preserved` | 保留を挟んでも受付時刻の順序が保たれる（遷移の不変条件） | 9.12 (4) |
| `tick_idempotent` | 同じ時刻の `tick` は状態を変えない（遷移の不変条件） | 9.12 (5) |
| `terminal_holds_no_table` | 終端状態のチケットはテーブルを持たない | 追加 |
| `table_occupant_is_consistent` | テーブルの `occupantTicketId` とチケットの `tableId` が双方向に一致する | 追加 |
| `held_table_has_deadline` | `HELD` のテーブルに対応するチケットは `holdDeadline` を持つ | 追加 |
| `unique_ids` | チケット ID とテーブル ID がそれぞれ一意 | 追加 |
| `code_uniqueness` | 有効なチケットの表示コードが一意 | 追加 |

`no_starvation` は割当の直後にしか成立しない（呼び出し中は一時的に破れうる）ため、**適用の出口で検査する不変条件**と**割当の直後にだけ検査する不変条件**を分けて宣言する。この区別を型で表す。

**テスト**

各不変条件について「成立する状態」と「破れる状態」を手で作り、検査が正しく判定することを確認する。破れる状態を作れない不変条件は、型で保証されているということなので、その旨をコメントに残して宣言から外す。

**完了条件**: 宣言されたすべての不変条件に、成立例と違反例のテストがある。

**実装後の記録（2026-09-17）**

**10 個の想定に対して 14 個になった。** 本書の当初の一覧に、実装しながら必要と分かった 4 つを足した。

| 追加した不変条件 | なぜ要るか |
|---|---|
| `assignment_status_matches` | ID が一致していても、席の状態が `FREE` のままなら別の人に割り当てられてしまう。ID の一致（`table_link_is_mutual`）だけでは足りない |
| `assigned_party_fits_capacity` | 割当が保証するが、人数の変更（7.6）や席の定員の変更で破れうる |
| `state_timestamps_are_set` | `CALLED` なのに期限が無い、終端なのに終わり方が無い、といった穴を落とす |
| `end_reason_matches_state` | PR 1 で宣言した `END_REASON_STATES` との食い違いを、統計に混ざる前に落とす |

当初の一覧にあった `unique_ids` は 1 つにまとめ（チケットとテーブルの両方を検査）、`held_table_has_deadline` はそのまま残した。

**3 つの群に分けた。** 型で区別することは本書が求めていたとおりで、実際に `no_starvation` は割当の直後にしか成立しない。常時検査の群に混ぜると、席が解放された直後や呼び出しの途中の正常な状態を違反として弾いてしまう。

| 群 | 数 | いつ検査するか |
|---|---|---|
| `STATE_INVARIANTS` | 11 | すべてのコマンド適用と `tick` の出口 |
| `POST_ALLOCATION_INVARIANTS` | 1 | 割当を実行した直後にだけ |
| `TRANSITION_INVARIANTS` | 2 | 遷移の前後の 2 状態について |

**`tick_idempotent` の表し方。** 9.12 の 5 は関数の性質であって状態の述語ではない。そこで「渡された 2 つの状態が同じ内容であること」という遷移の不変条件として宣言し、同じ時刻で `tick` を 2 回呼んだ結果を渡す責務は呼び出し側（PR 12）に置いた。これに必要な `sameVenueState` を `domain/state.ts` に足してある。PR 12 の「1 時間を 1 回で進めた場合と 360 回で進めた場合が一致する」テストでも使う。

**`priority_preserved_across_pause` の表し方。** 9.12 の 4「保留を挟んでも受付時刻の順序は保たれる」をそのまま述語にはできない。人数の増加や `requeue_back` は順番を意図的に変えるためである。そこで「保留に入る遷移と保留から出る遷移では `priorityAt` が変わらない」と言い換えた。状態の組だけから検査でき、7.7 の「譲っても損をしない」という設計判断をそのまま守る。
---

### 段 B — 基本の循環とシミュレータ

---

#### PR 4 — 割当の選択

> **問い: 割当は 7.6 のとおりか。数値例とエッジケース表を再現できるか。`fairness_override_min` を振ると FIFO と純 best fit の両端になるか。**

**出典**: 全体プラン 7.6
**依存**: PR 1 ／ **見積**: 6h ／ **目安**: 実装 450 行 ／ **実測**: 実装 98 行・テスト 722 行（69 テスト）

**実装**

`packages/core/src/allocation/choose.ts`。

```ts
export interface Assignment {
  readonly ticketId: TicketId;
  readonly tableId: TableId;
}

/** 状態を変えない。誰をどの席に案内すべきかを決めるだけ。適用は PR 6。 */
export function chooseAssignments(state: VenueState, now: Timestamp): readonly Assignment[];
```

**状態を変えないことがこの PR の設計上の要点である。** 選択と適用を分けることで、7.6 のエッジケース 9 行を、状態遷移の都合に邪魔されずにテストできる。

内訳:

1. **テーブルの処理順**: `capacity` 昇順 → `verifiedFreeAt` 降順 → `adminRank` 昇順 → `label` 昇順。`policy.table_order` で切り替えられるようにする（8.2 の比較項目 5 のため）
2. **候補の絞り込み**: `state === 'WAITING'` かつ `partySize <= capacity` かつ希望タグを満たす
3. **選択**: ロス（`capacity - partySize`）が最小の候補群から、`priorityAt` が最も早い人（best fit）
4. **公平性オーバーライド**: 候補の中で最も長く待っている人が、best fit の人より `fairness_override_min` 以上長く待っていれば、その人を優先
5. **`conflictPriority`** が立っているチケットは、同じ `priorityAt` の中で最優先

**テスト（この PR の価値はテストにある）**

- **7.6 の数値例 2 つを、そのまま 2 本のテストとして書く**
  - 4 人席 1 卓、A=2 名 10 分待ち、B=4 名 3 分待ち → B に案内
  - 同条件で A が 15 分待ち → A に案内
- **7.6 のエッジケース表 9 行を、1 行 1 テストで写す**
  - 最大定員超過 / 待ちが空で空席あり / 待ち先頭が `PAUSED` / 空席が複数で待ちが 1 組 / 人数の変更 / 席の設定変更 / 制約付きの人 / 待ちの上限 / 割当後により良い席（再割当しない）
- **退化のテスト**
  - `fairness_override_min = 0` で厳密な FIFO になること
  - `fairness_override_min = Number.POSITIVE_INFINITY` で純 best fit になること
  この 2 つが 7.6 の「施設が連続的に調整できる」という主張の検証になる
- **決定性**: 同じ状態に対して常に同じ結果。配列の順序に依存しないこと（入力をシャッフルしても同じ）
- **性質テスト**: 返された割当の中で、同じテーブルが 2 回現れない。同じチケットが 2 回現れない。すべての割当で `partySize <= capacity`

**完了条件**: 7.6 の数値例とエッジケース表がすべてテストとして存在し、緑。

**実装後の記録（2026-09-17）**

**性質テストが、私が書いたテストの誤りを 2 回捕まえた。** どちらも実装ではなくテスト側の思い込みで、2 つ目は 7.6 の設計判断の根拠そのものを示していた。

| 誤った仮定 | 実際 |
|---|---|
| 先着順（`fairnessOverrideMin = 0`）では理由が `best_fit` にならない | 最も早い人が同時にロス最小のときは `best_fit` になる。理由の値では方針を判定できないので、「候補の中で最も早い人が選ばれているか」を割当の順にたどり直して検証する形に書き直した |
| 席の処理順を変えても案内できる人数は変わらない | **変わる。** 大きい席を先に処理すると、公平性の繰り上げで小さい組がそこに入り、大人数が座れなくなる。具体例を `choose.test.ts` に残した |

2 つ目は 7.6 の「テーブルは定員の小さい順に処理する（大きい席を大人数のために残す）」という規則の理由を、実行できる形にしたものになった。4 人席と 2 人席が 1 卓ずつ、待ちが 4 名（1 分）と 2 名（30 分）のとき、定員昇順なら 2 組とも案内でき、大きい席から処理すると 1 組しか案内できない。

**この PR で決めたこと**

- **`chooseAssignments` は現在時刻を受け取らない。** 本書の当初の署名は `(state, now)` だったが、公平性の判定で使う 2 人の待ち時間の差は、どちらも「現在時刻 − 受付時刻」なので現在時刻が打ち消し合う。受付時刻の差だけで決まるため、`now` は使われない。使わない引数を残さない方針とし、必要になった PR で足す。
- **`Assignment` に選ばれた理由（`only_candidate` / `best_fit` / `fairness_override`）を持たせた。** 7.6 が求める「なぜ後から来た 4 人組が先に案内されたのか」を一文で説明するために要る。シミュレーションの統計（8.3）でも使う。
- **席の並びは最後に ID で決着させる。** `tableOrder` の鍵で決着しない場合に入力の配列順で結果が変わらないようにするため。決定性は性質テストで守る。

**規模**: 実装 98 行は目安の 450 行を大きく下回り、テストが 722 行とその 7 倍になった。選択そのものは短く、費用はテスト側にある。仕様の表を 1 行 1 テストで写す方針（原則 5）がそのまま効いた形で、PR 1・2 とは逆の偏りになった。

---

#### PR 5 — `apply` の骨格と受付・キャンセル・保留

> **問い: `apply` は必ず遷移表と不変条件を通るか。受付の検証は 7.5 のとおりか。**

**出典**: 全体プラン 7.5（受付）、7.9（キャンセル）、7.7 の 5〜7（保留） ／ CLAUDE.md 3.1、3.2
**依存**: PR 2、PR 3 ／ **見積**: 6h ／ **目安**: 500 行

**実装**

`packages/core/src/machine/apply.ts`。

```ts
export type Command =
  | { readonly type: 'JOIN'; readonly ticketId: TicketId; readonly partySize: number;
      readonly requiredTags: readonly string[]; readonly hasNotificationChannel: boolean }
  | { readonly type: 'CANCEL'; readonly ticketId: TicketId; readonly by: Actor; readonly reason?: CancelReason }
  | { readonly type: 'PAUSE'; readonly ticketId: TicketId }
  | { readonly type: 'READY'; readonly ticketId: TicketId }
  | { readonly type: 'CHANGE_PARTY_SIZE'; readonly ticketId: TicketId; readonly partySize: number }
  | { readonly type: 'HEARTBEAT'; readonly ticketId: TicketId };

export function apply(
  state: VenueState, command: Command, now: Timestamp,
): Result<Decision<VenueState, DomainEvent>, Rejection>;
```

**`apply` が必ず通る手順**を、この PR で骨格として固定する。以降の PR はこの骨格にコマンドを足すだけにする。

1. コマンドの前提条件を検査する（対象のチケットが存在するか、など）
2. 遷移表を引く。表に無ければ拒否する
3. ガードを評価する。満たさなければ拒否する
4. 状態を更新する（新しいオブジェクトを返す）
5. **不変条件を検査する。破れていたら変更を破棄して拒否する**
6. イベントを返す

**失敗の扱い**: 例外ではなく `Result` を返す。理由はコード（`PARTY_TOO_LARGE`、`QUEUE_FULL`、`JOIN_CLOSED`、`NOT_ALLOWED_IN_STATE` など）で表し、利用者向けの文言は Phase 2 の i18n に置く。純粋関数の失敗を型で表現でき、呼び出し側が握り潰しにくくなる。

受付の検証（7.5）:

| 検証 | 拒否コード |
|---|---|
| `1 <= partySize <= max_party_size`（対象席の最大定員） | `PARTY_TOO_LARGE` / `PARTY_TOO_SMALL` |
| 待ち行列が `max_queue_length` 未満 | `QUEUE_FULL` |
| 受付が開いている（`joinOpen`、7.14 は PR 11） | `JOIN_CLOSED` |

人数の変更（7.6 のエッジケース）: 減らす場合は `priorityAt` を保持、増やす場合は `priorityAt` を現在時刻に更新する。

キャンセル（7.9）: `WAITING` / `PAUSED` / `CALLED` のいずれからも可能。`CALLED` からのキャンセルはテーブルを `FREE` に戻す。理由を `EndReason` に記録する。

保留（7.7 の 5〜7）: `PAUSE` で `PAUSED` へ、`READY` で `WAITING` へ。`priorityAt` は保持する。保留の期限（`pause_step_min`、`pause_max_total_min`）を設定する。期限切れの処理は `tick` 側（PR 6）。

**ファズの生成器をここで導入する。** fast-check で、この時点で存在するコマンドのランダムな列を生成し、不変条件が破れないことを確認する。**以降のすべての PR で、追加したコマンドを生成器に足す。**

**テスト**

- 受付の検証 3 種それぞれの境界値（`partySize` が 0、1、上限、上限 +1）
- `WAITING` / `PAUSED` / `CALLED` からのキャンセル
- 保留と準備 OK の往復で `priorityAt` が変わらないこと
- 人数の増減で `priorityAt` がどう変わるか
- 表に無い遷移（`DONE` からの `PAUSE` など）が拒否されること
- 不変条件が破れるようなコマンドを意図的に作り、拒否されて状態が変わらないこと
- ファズ: 500 ケースのランダムなコマンド列で不変条件違反ゼロ

**完了条件**: `apply` の骨格が固定され、ファズが緑。

**実装後の記録（2026-09-17）**

**着手前に全体プランを 2 か所直した**（横断ルール 6.4）。PR 2 と同じく、7.3 の図が本文より先に書かれていたことによる食い違いだった。

| 箇所 | 食い違い | 直し方 |
|---|---|---|
| 7.3 の図 | 7.7 の 7 と 7.3 の画面表が「保留中の[延長]」を求めているのに、図に `PAUSED → PAUSED` の矢印が無い | 図に追加し、遷移表にも 1 行足した（25 → **26 本**）。コマンドの実装は PR 6 |
| 7.9 の表 | 「本人の明示的キャンセルはどの状態からでも可」が、`SEATED` からの `CANCEL` を持たない 7.3 の図と食い違う | 「待っている間（`WAITING` / `PAUSED` / `CALLED`）なら」と書き改めた。着席後は退席で扱う |

**ファズが誤りを 1 件捕まえた。** 私が書いた性質「保留の合計時間が上限を超えない」は **偽**だった。上限は期限という 1 つの仕掛けで守っているが、期限切れにするのは `tick`（PR 6）なので、次の `tick` までに本人が「準備OK」を押せばその分だけ合計が上限を超える。実運用の `tick` は 10 秒ごとなので超過はその範囲に収まる。`apply` が守れるのは「1 回に渡す持ち時間」までで、性質をそちらへ書き直し、超過する具体例をテストに残した。

**この PR で決めたこと**

- **`Ticket` に `pausedSince` を足した。** PR 1 の `pausedTotal`（保留していた時間の合計）は、保留に入った時刻を持たないと測れない。無いままだと「保留に入った回数 × `pauseStepMin`」で近似するほかなく、早く戻った人が損をする。不変条件 10 を「`PAUSED` のときだけ入っている」と両方向で見るよう強めた。
- **保留の期限は `now + min(pauseStepMin, 残りの持ち時間)`。** 1 つの仕掛けで 2 つのパラメータを守る。持ち時間が尽きた人の期限は現在時刻になり、次の `tick` で期限切れになる。`PAUSE` を拒否する案も考えたが、`HOLD_EXPIRE → PAUSED`（7.7 の 6）が同じ状況を作るため、入口だけ塞いでも一貫しない。
- **心拍と人数の変更は遷移表に入れない。** どちらも状態を変えず、チケットの欄だけを書き換える。代わりに `HEARTBEAT_APPLIES_TO` / `PARTY_SIZE_CHANGE_APPLIES_TO` として適用範囲を宣言した（`MAX_AGE_APPLIES_TO` と同じ形）。図を自己遷移で埋めずに、「どの状態で受け付けるか」はデータのまま保てる。
- **人数の変更は `WAITING` / `PAUSED` だけ。** `CALLED` と `SEATED` で認めると、確保済みの席の定員を超えて `assigned_party_fits_capacity` が破れる。7.6 の「人数の変更（待ち中）」の「待ち中」をこう解釈した。
- **心拍はイベントを出さない。** 数秒ごとに届くため、永続化すると価値の無い行で埋まる。放置の判定に要るのは最後の時刻だけで、それは状態が持つ。
- **表示コードは使用中のものを飛ばして採番する。** カウンタを進めるだけでは一巡したときに `unique_active_codes` が破れ、受付ができなくなる。`allocateTicketCode` を足した。
- **`apply` は不変条件を毎回検査する。** CLAUDE.md 3.2 の「本番では抽出検査」は境界側の判断として Phase 2 に残す。席 100・チケット 200 の規模では 1 回あたり数万回の比較で済むため、実測せずに抜き取りへ落とさない。
- **未実装のガードは通さず、`GUARD_NOT_IMPLEMENTED` で区別する。** この PR の遷移はすべて無条件なので、実装済みのガードは 0 個である。`unimplementedTicketGuards()` が残りを数え、PR 12 で 0 になる。

**骨格が守っていること（以降の PR が壊せないもの）**

- 手順 5（不変条件の検査）は飛ばせない。コマンドごとの処理は module 内に閉じ、`apply` だけを export してある
- 手順 2（遷移表）を飛ばしていないことは性質テストが見る。**状態が動いたすべてのチケットについて、その（元 → 次）が表に宣言されていること**を確かめるので、状態を直接書き換える処理を足せばそこで落ちる
- **状態機械が動いたなら必ずイベントが出る。** 先に全イベントを宣言しておく案（本書 7.5）より、この性質のほうが出し忘れを止められる。イベントはいま出しているものだけを宣言する方針にした
- ファズの生成器が `COMMAND_TYPES` をすべて覆っていることをテストで見る。コマンドを足して生成器に足し忘れれば落ちる

**その他**: `Result` を `packages/core/src/result.ts` に置き、`decision.ts` の `Apply` を `Result` を返す形に改めた（本書 7.3 の決定が Phase 0 の型に反映されていなかった）。PR 4 が公開していた型 `Pick` は TypeScript 標準の `Pick<T, K>` を隠すため `CandidatePick` に改名した。

**規模**: 実装 489 行（目安 500 行）、テスト 1,312 行・106 テスト。累計 676 テスト。付録 12.1 の見込みに対し、`command.ts`・`rejection.ts`・`result.ts` の 3 ファイルが増えた（`apply.ts` に詰め込むと 1 ファイルが 600 行を超えるため）。

---

#### PR 6 — 呼び出し・ホールド・ノーショー

> **問い: 7.7 の図（呼び出し → リマインド → 期限 → 延長）が実装されているか。3 つのノーショー方針が切り替わるか。**

**出典**: 全体プラン 7.7、7.16
**依存**: PR 4、PR 5 ／ **見積**: 6h ／ **目安**: 500 行

**実装**

PR 4 の選択結果を状態に反映する部分と、時刻起因の遷移を `tick` に足す部分の 2 つ。

- **割当の適用**: `chooseAssignments` の結果を `WAITING → CALLED` と `FREE → HELD` に反映し、`holdDeadline = now + hold_min` を設定する。割当を実行するきっかけ（受付・準備 OK・退席・設定変更）を洗い出し、1 つの内部関数に集約する
- **`tick` に足すもの**: リマインドの発火（`hold_reminder_before_min` 前）、ホールドの期限切れ、保留の期限切れ、チケットの絶対上限（`ticket_max_age_min`）、放置（`abandon_timeout_min`、通知手段が無い場合のみ）
- **コマンド**: `EXTEND`（向かっています）、`PASS`（譲る）
- **保留の延長**: `PAUSED --EXTEND--> PAUSED`（PR 5 で遷移表に足した行）。期限を `now + min(pause_step_min, 残りの持ち時間)` に延ばす。`CALLED` からの `EXTEND`（ホールドの延長）と事象名は同じで、起点で区別する
- **ノーショーの 3 方針**: `requeue_once`（1 回目は `PAUSED` で順番保持、2 回目で `NO_SHOW`）、`cancel`（1 回で終了）、`requeue_back`（`priorityAt` を現在時刻にして末尾へ）

**テスト**

- 7.7 の図をそのまま時系列でたどるテスト（0 分で呼び出し、5 分でリマインド、7 分で期限、延長すると 10 分）
- 延長の回数制限（`max_extensions` を超えたら拒否）
- パスで席が即 `FREE` に戻り、次の人に割り当てられること
- ノーショー 3 方針それぞれの分岐
- `requeue_once` で 2 回目の期限切れが `NO_SHOW` になること
- 保留の期限切れが `EXPIRED` になること、操作があれば延長されること
- 放置判定が、通知手段がある人には適用されないこと
- ホールド中の席が他の人に割り当てられないこと（不変条件で担保されるが、明示的にテストする）

**完了条件**: 7.7 の 8 項目すべてに対応するテストがある。

**実装後の記録（2026-09-17）**

**7.7 の 7 の「15 分」を決着させた。** 15 という数は 7.16 のどのパラメータにも対応していなかった。保留の期限は `pause_step_min`（10 分）だけで決め、別の猶予は設けないことにして、7.7 の本文を書き直した。理由は、保留に関わる数を 3 つ（1 回分・合計・猶予）にすると一文で説明できなくなるため（全体プラン 7.1）。画面には残り時間と[延長]が出ているので、猶予は要らない。

**ファズが実装のバグを 2 件見つけた。** どちらも手で書いたテストでは通っていた。

| バグ | 何が起きていたか | 直し方 |
|---|---|---|
| 保留の合計上限が効かない | `pausedTotal` は保留を **閉じるとき** にしか増えない。続いている保留の経過を見ていなかったので、「延長」を押すたびに上限いっぱいの持ち時間が戻り、いつまでも保留していられた | `pauseWindowEnd()`（保留に入った時刻＋使い残し）を足し、期限をそこで頭打ちにした |
| 同じ知らせが繰り返し出る | 延長のたびに「知らせた」記録を無条件に消していた。`hold_extension_min` が 0 の施設では期限が動かないので、記録だけが消えて同じ知らせが何度も出た | 期限が **実際に動いたときだけ** 記録を消す |

**ファズが性質の書き方も 2 回正した。** どちらも私の側の思い込みだった。

- **1 回の `apply` は 1 枚のチケットを 2 歩動かす。** 「準備OK」は `PAUSED → WAITING` と動かし、続く割当が `WAITING → CALLED` まで進める。前後だけを見ると表に無い遷移に見える。割当の一歩は `TicketCalled` が出ていることで見分け、コマンドの一歩を表と突き合わせる形に書き直した
- **「時間の進め方によらず同じ状態になる」は、条件付きでしか成り立たない。** 成り立つのは (1) あいだにコマンドが挟まらず、(2) 空席が無いときだけ。心拍が届けば放置の起点が動くので「心拍と `tick` のどちらが先か」で結果が変わるし、呼び出しは「いま」起きる行為なので `tick` の間隔に依存する。**どちらも設計上のもので消せない。** PR 12 はこの条件付きの形で検証すること

**この PR で決めたこと**

- **割当は `apply` と `tick` の手順に組み込む。** きっかけ（受付・準備OK・パス・取り消し・退席・設定変更・時刻の経過）が多すぎて、コマンドごとに呼び分けると必ずどれかを忘れる。忘れた症状は「空席があるのに誰も呼ばれない」で、次の操作で勝手に直るため気づきにくい。**副産物として `no_starvation` が出口で検査できるようになった**（PR 3 で常時検査から外した条件が戻ってきた）
- **期限はその期限の時刻で処理する。** 7 分で切れたホールドは、30 分後に気づいても「7 分に切れた」として刻む。現在時刻で刻むと、そこから置かれる次の期限が現在時刻より先になり、まとめて処理したときに連鎖が途切れる
- **期限は早いものから順に処理する。** 保留の期限（45 分）と受付からの上限（90 分）の両方を過ぎたチケットを 100 分後に処理するとき、種類の順で決めると `max_age` になるが、10 秒ごとに呼んでいれば `pause_expired` になる。早い順なら、どちらでも `pause_expired`
- **ガードは時間を見ない。** 「どちらへ進むか」はガードが決め、「いつ進むか」は期限が決める。両方が時刻を見ると、期限の時刻ちょうどで処理したときに境界で食い違う（`noNotificationChannel` から時間の判定を外した）
- **`tick` は `Result` を返す。** `apply` と出口（割当と不変条件の検査）を共有するので、失敗の伝え方も揃える。ただし **`tick` の失敗は必ず実装の誤り**で、業務上の理由で拒否することはない。性質テストが「`tick` は拒否しない」を見ている
- **終わりのイベントは `TicketEnded` 1 つにまとめた。** PR 5 の `TicketCancelled` を置き換えた。終わり方は 10 通りあるが、消費者が知りたいのは「終わった」ことと「なぜ」で、そこは `endReason` が語る。終わり方ごとに分けると、足すたびに消費者側の分岐が増える
- **延長の回数は呼び出しごとに数え直す。** 「1 回の呼び出しにつき 1 回まで」と一文で説明できる。譲ったあとの呼び出しでもう一度使える
- **席が塞がっていた人の繰り上げ（`conflictPriority`）は、呼び出した時点で消す。** 席を案内することが繰り上げの果たされ方である。案内を受けたあとで自分から譲った人が持ち続けることはない（PR 9 で再確認すること）

**ファイルの分け方**: `apply.ts` から遷移の段を `transition.ts` へ、期限の計算を `deadlines.ts` へ、終わらせ方と席の解放を `release.ts` へ出した。割当の適用は `allocate.ts`。`apply` と `tick` が同じ部品を使うためで、`apply.ts` に置いたままだと互いを読み合う形になる。`settle`（割当と検査の出口）は公開しない。外から見える入口は `apply` と `tick` の 2 つだけにしてある。

**規模**: 実装は新規 398 行＋既存への追加。テスト 1,080 行・91 テスト。累計 777 テスト。ガードは 10 個中 6 個が実装済みになった（残り 4 つは PR 7・9・10）。

---

#### PR 7 — 着席と退席

> **問い: 正常系の着席と退席が通るか。退席した席が次の人に渡るか。**

**出典**: 全体プラン 7.8 の 1 行目、7.11 の 1 層目
**依存**: PR 6 ／ **見積**: 4h ／ **目安**: 350 行

**実装**

- `CHECK_IN`: 割り当てられたテーブルに対する着席確認。`CALLED → SEATED`、`HELD → OCCUPIED`。テーブルのトークン（または 4 桁コード）の照合はここで行う
- `CHECK_OUT`: `SEATED → DONE`、`OCCUPIED → TURNOVER`。`verifiedFreeAt` を更新する
- `TURNOVER → FREE`: `turnover_min` の経過で（`tick`）。既定は 0 なので即時。`deadlines.ts` に片付けの期限を足し、`tick.ts` の期限の一覧に加える
- 退席後の割当は **書かなくてよい**。PR 6 で `apply` と `tick` の手順に組み込んだので自動で走る
- 席のガード（`stillManaged`、`disableAfterCurrent`）をここで実装する。`TURNOVER_DONE` の 2 本の行が使う

**この PR で基本の循環が閉じる。** 受付 → 割当 → 呼び出し → 着席 → 退席 → 再割当。PR 8 のシミュレータはここから回せる。

**テスト**

- 一周を通すテスト（1 卓、1 人）
- 別の席のトークンで着席しようとしたら拒否されること（分岐の実装は PR 9、ここでは拒否でよい）
- 退席した席が次の待ち人に割り当てられること
- `turnover_min` を 1 分にすると、1 分間は次に割り当てられないこと
- `verifiedFreeAt` が更新され、7.6 の並び順に反映されること

**完了条件**: 受付から退席までの一周が通り、席が循環する。

**実装後の記録（2026-09-17）**

**基本の循環が閉じた。** 受付 → 割当 → 呼び出し → 着席 → 退席 → 再割当。PR 8 のシミュレータはここから回せる。

**この PR で決めたこと**

- **期限に 2 種類あることを型の上で分けた。** `hasPassed()`（利用者に与える猶予。ちょうどは過ぎていない）と `reached()`（設備の都合の待ち。ちょうどで終わり）。**片付けの猶予を 0 分にしたときに、本当に待ち時間ゼロになるようにするため。** `hasPassed()` で測ると、猶予 0 分でも次の `tick` まで席が空かず、誰の利益にもならない待ちができる。ホールドの 7 分は `hasPassed()` のままで、7 分ちょうどに着いた人はまだ間に合う
- **席の期限は `settle`（`apply` と `tick` の共通の出口）が見る。** チケットの期限は `tick` だけが見る。分けた理由は、チケットの期限が利用者の操作と競合するのに対し、席には意思が無く、時計が進めば必ず明けているから。**割当の前に明かしておかないと、空いているはずの席が次の人に渡らない。** この判断のために `settle` を `apply.ts` から `settle.ts` へ出した
- **`verifiedFreeAt` は退席の申告の時点で更新する。** 片付けの猶予が明けた時点ではない。「席が空いている」ことの手がかりとして、本人の申告がもっとも新しい証拠になる（7.6 の席の並び順）
- **着席では席との結びつきを残し、ホールドの期限だけを外す。** `clearedHold()` を `clearedHoldDeadline()` と分けた
- **スタッフの退席申告に理由を求めない。** 取り消し（7.9）と違い、席を空けることは利用者の不利益にならず、監査で問われるのは「誰が」だけである
- **対象外にする操作の保留（`disableAfterCurrent`）は、片付けの猶予が明ける瞬間に反映する。** `enabled` を落とし、予約も消す。予約を **立てる** コマンドは Phase 1 には無い（管理画面は Phase 2）ので、遷移だけを先に実装した

**ファズは今回、誤りを見つけなかった。** PR 4・5・6 と続いた指摘が止まったのは、PR 6 で入れた「期限はその期限の時刻で処理する」「席の期限は出口で明かす」という形が、この PR の追加とかみ合っていたためだと思われる。生成器には着席と退席を足し、**受付から退席までの一周をランダムに回せるようにした**（PR 8 の下地）。

**足した性質**: 「猶予が明けた席が片付け中のまま残らない」「着席している人は必ず席を持ち、その席も本人を指す」「終わり方はこの版が作れる 8 通りのいずれかになる」。3 つ目は、宣言されている 10 通りのうち残る 2 つ（`auto_release`・`venue_closed`）が PR 10・11 のものであることも同時に確かめている。

**規模**: 実装は `settle.ts` 102 行（うち大半は `apply.ts` からの移動）と、既存への追加。テスト 386 行・30 テストを `seating.test.ts` に追加し、性質テストも増やして累計 821 テスト。ガードは **チケット 10 個中 7 個、席 3 個中 2 個** が実装済みになった（残るのは `earlyCheckInAllowed`・`swapAllowed`・`hardLimitMode`・`autoFreeEnabled`）。

---

#### PR 8 — シミュレータの骨格

> **問い: 8.1 のモデルを再現できるか。同じシードで同じ結果になるか。ランダムな負荷で不変条件が破れないか。**

**出典**: 全体プラン 8.1、8.5 ／ 9.12（ファズ）
**依存**: PR 7 ／ **見積**: 7h ／ **目安**: 550 行

**この PR の位置づけは 3.2 の判断 3 に書いた。以降のすべての PR は、このシミュレータに叩かれる。**

**実装**

`packages/core/sim/` に置く。**`packages/core` と同じく依存ゼロを守る。**

| 部品 | 内容 |
|---|---|
| `rng.ts` | 決定的な擬似乱数（xorshift128+ など）。シードから再現できること。`Math.random()` は使えない（使わないことが CI で検査される） |
| `distributions.ts` | 非定常ポアソン過程（到着）、離散分布（人数）、対数正規分布（滞在時間、呼び出し→着席）、ベルヌーイ（ノーショー、退席申告） |
| `scenario.ts` | 席構成とパラメータ。8.1 の初期値を `weekday-lunch` / `weekend-peak` として定義。席構成は 26 席・50 席・100 席 |
| `runner.ts` | イベントループ。仮想時刻を進め、コマンドを core に投入し、`tick` を 10 秒間隔で呼ぶ |
| `agent.ts` | 利用者の振る舞い。呼び出しに応じるか、いつ着席するか、退席を申告するか |

**確率的な判断はすべてシミュレータ側に置く。** core は完全に決定的で、乱数を必要としない設計を保つ。「この人はノーショーする」という判断はシミュレータが下し、core にはコマンド（または何もしないこと）として現れる。

この PR の範囲では、PR 7 までの振る舞い（受付・割当・呼び出し・着席・退席・ノーショー）だけをモデル化する。無断利用・飛び込み・時間上限は、対応する PR（9、10）で足す。

**テスト**

- **決定性**: 同じシードで 2 回走らせると、イベント列が完全に一致する
- **分布**: 生成された人数の分布が 8.1 の指定（1 名 25%、2 名 40%…）に統計的に一致する。到着間隔が指定した率に一致する
- **仮想時刻の単調性**: 時刻が巻き戻らない
- **不変条件**: 100 回の実行で違反ゼロ

**完了条件**: `weekend-peak` シナリオが 1 日分（3.5 時間）を数秒で走り、不変条件が破れない。
---

### 段 C — 枝葉と時刻

ここから 3 つ（PR 9、10、11）は互いに独立している。順不同でよい。

---

#### PR 9 — 座席 QR の全分岐と飛び込み

> **問い: 7.8 の表の 10 行すべてが実装され、テストされているか。**

**出典**: 全体プラン 7.8、7.12、7.11 の 3 層目
**依存**: PR 7 ／ **見積**: 6h ／ **目安**: 500 行

**実装**

座席 QR は受付・着席・退席・報告のすべてを兼ねる。**読み取った人の状態とテーブルの状態の組み合わせで振る舞いが決まる**ため、この分岐を 1 か所にまとめ、7.8 の表と 1 対 1 で対応させる。

```ts
/** 座席 QR を読んだときに、その人に何を見せ、何ができるかを決める。 */
export function resolveTableScan(
  state: VenueState, tableId: TableId, ticketId: TicketId | null, now: Timestamp,
): TableScanOutcome;
```

`TableScanOutcome` は判別可能なユニオンにし、7.8 の表の行と 1 対 1 にする。画面の文言は Phase 2 だが、**どの結果になるかは core が決める**（CLAUDE.md 3.1: ビューは業務判断をしない）。

実装する分岐（7.8 の表の順）:

1. 割り当てられた席 → 着席（PR 7 で実装済み）
2. 別の席に割当、その席が空いていて人数が収まる → 席の変更（`allow_table_swap`）。元の席は `FREE` に戻る
3. 別の席に割当、その席は使えない → 自分の席を案内するだけ
4. `WAITING` で、空席かつ人数が収まり、その席を待つ人が他にいない → 即着席（割当の前倒し）
5. `SEATED` でこの席 → ご利用中の画面
6. チケットなし、空席、収まる待ちがいない → **飛び込み着席**（7.12）
7. チケットなし、空席、待ちがいる → 受付へ誘導
8. チケットなし、`HELD` → 呼び出し中の席である旨
9. チケットなし、`OCCUPIED` / `OCCUPIED_UNKNOWN` → 使用中である旨、空席なら報告できる
10. 割り当てられた席だが誰かが座っていた → **席が塞がっていた報告**。テーブルは `OCCUPIED_UNKNOWN`、チケットは `WAITING` に戻り `conflictPriority` が立つ

**テスト**

**7.8 の表の 10 行を、1 行 1 テストで写す。** これがこの PR の完了条件そのものになる。加えて、

- 席の変更で元の席が解放され、次の人に渡ること
- `allow_table_swap = false` のとき変更が拒否されること
- 飛び込み着席で定員を超える人数が拒否されること
- 「席が塞がっていた」報告の後、その人が最優先で次の席に案内されること
- `conflictPriority` が同じ `priorityAt` の他者より前に来ること（PR 4 の選択と結合するテスト）

**完了条件**: 7.8 の表の 10 行すべてにテストがあり、緑。

---

#### PR 10 — 時間上限と整合性回復

> **問い: 7.10 の 3 モードが切り替わるか。7.11 の 5 層のうちロジックが担う 3 つ（問いかけ、確認要の割当、時間経過による整理）が動くか。**

**出典**: 全体プラン 7.10、7.11
**依存**: PR 7 ／ **見積**: 6h ／ **目安**: 500 行

**実装**

**時間上限（7.10）**

| モード | 動作 |
|---|---|
| `off` | 何もしない |
| `soft`（既定） | 上限を目安として扱う。超過しても自動解放しない。上限 ＋ `overstay_grace_min` でテーブルを `NEEDS_CHECK` に |
| `hard` | 上限 ＋ 猶予でチケットを `DONE` にし、テーブルを `NEEDS_CHECK` に |

`limit_only_when_waiting`（既定 true）が立っているときは、**待っている人がいない間は上限を評価しない**。これは 7.10 の「待っている人がいないときは誰も急かさない」という設計判断の実装である。待ちの有無が変わるたびに評価がオン・オフされるため、`tick` の中で毎回判定する。

**整合性回復（7.11）**

| 層 | この PR で実装するもの |
|---|---|
| 2. 問いかけ | 滞在が `still_here_prompt_at` を超えたら「まだご利用中ですか」のイベントを 1 回だけ発火。`still_here_timeout_min` 無応答で `NEEDS_CHECK` |
| 3. 確認要の割当 | `assign_needs_check` が真で、確実な空席が無いとき、`NEEDS_CHECK` の席を待ちの先頭に案内する。**通常の割当とは区別して扱う**（結果の型を分ける） |
| 5. 時間経過による整理 | `OCCUPIED_UNKNOWN` → `NEEDS_CHECK`（`unknown_occupancy_to_check_min`）、`NEEDS_CHECK` → `FREE`（`needs_check_auto_free_min`、`off` 可） |

1 層目（申告を最小の手間にする）と 4 層目（スタッフ画面）は画面の仕事なので Phase 2 と Phase 3。

**テスト**

- 3 つの上限モードそれぞれの挙動
- `limit_only_when_waiting` が真のとき、待ちがいない間は超過しても何も起きないこと。待ちが発生した瞬間に評価されること
- `hard` モードで自動解放された席が `NEEDS_CHECK` になり、`FREE` にはならないこと（次の人に「確実な空席」として案内しないため）
- 問いかけが 1 回だけ発火すること（繰り返さない）
- `assign_needs_check` の案内が、確実な空席があるときには起きないこと
- `NEEDS_CHECK` が自動解放されるまでの時間、`off` にすると解放されないこと
- **席が永久に塞がらないこと**（性質テスト: 任意の状態から時間を十分に進めると、すべてのテーブルが `FREE` か `DISABLED` に到達する）

最後の性質テストは 7.11 の「最悪でも席が永久に塞がらない」という主張の検証で、この PR の価値の中心にある。

**完了条件**: 7.10 の 3 モードと 7.11 の 3 層にテストがあり、「席が永久に塞がらない」性質が緑。

---

#### PR 11 — 運用時間帯とモード切替

> **問い: 運用時間の開始と終了で、席が正しく切り替わるか。全席解放が安全に動くか。**

**出典**: 全体プラン 7.14
**依存**: PR 5 ／ **見積**: 4h ／ **目安**: 350 行

**実装**

- **運用スケジュール**: 曜日と時間帯の指定（`managed_schedule`）。施設のタイムゾーンで評価する。core は時刻を UTC のミリ秒で受け取るため、タイムゾーンの変換は境界側で行い、core には「運用時間内かどうか」を判定できる形（施設のローカル時刻に変換済みの分数など）で渡す
- **運用開始**: 対象席が `DISABLED → FREE`
- **運用終了**: `join_cutoff_before_close_min` 前に受付を停止。終了時刻に残った `WAITING` / `PAUSED` を施設都合でキャンセル。`SEATED` はそのまま（座り続けてよい）
- **手動の切替**: スタッフによる運用 ON / OFF。スケジュールより優先する
- **全席解放**: 緊急時にすべてのチケットを `CANCELLED(施設都合)` にし、すべてのテーブルを `DISABLED` にする

**テスト**

- スケジュールによる開始と終了
- 受付停止のタイミング
- 終了時に `SEATED` のチケットが巻き込まれないこと
- 手動の切替がスケジュールより優先されること
- 全席解放の後、不変条件が成立していること
- 運用中に席を対象外にしたとき、`disableAfterCurrent` で現在の利用が終わってから反映されること（7.6 のエッジケース）

**完了条件**: 運用時間帯の開閉と全席解放がテストされ、シミュレータのシナリオが運用時間を持てるようになる。

---

#### PR 12 — `tick` の性質と遷移表の網羅

> **問い: 時間をどう飛ばしても、何度呼んでも、状態は壊れないか。宣言した遷移は全部動くか。**

**出典**: 全体プラン 9.4、9.12 ／ CLAUDE.md 3.4
**依存**: PR 6、9、10、11 ／ **見積**: 5h ／ **目安**: 400 行

`tick` は PR 6・10・11 で少しずつ育つ。この PR は新機能ではなく **固めるための PR** である。

**実装**

- 時刻起因の遷移を 1 つの `tick` に集約し、評価順を明示する（期限切れ → 自動整理 → 割当、の順）
- 大きな時間の飛びへの対応。サーバの再起動や時計のずれで、10 秒ではなく 1 時間飛ぶことがある。**期限を「経過した回数」ではなく「絶対時刻の比較」で判定する**設計になっていることを確認し、飛んでも取りこぼさないことを保証する
- **遷移表の網羅性の検査**: 宣言されたすべての遷移が実装から到達可能であること、ガード名の集合が実装済みガードの集合と一致すること

**テスト**

- **冪等性**: 同じ時刻で `tick` を 100 回呼んでも状態が変わらない（性質テスト）
- **時間の飛び**: 1 時間を、10 秒刻みで 360 回進めた場合と、1 回で進めた場合の結果が一致する（ただし途中で発火するイベントの個数は一致しなくてよい。到達する状態が一致すること）
- **単調性**: 時刻を戻す `tick` が拒否されること
- **網羅性**: `TICKET_TRANSITIONS` と `TABLE_TRANSITIONS` の全行が、実際のコマンドまたは `tick` で到達できること。到達できない行があれば、仕様の誤りか実装漏れなので落とす
- **ガードの一致**: 遷移表が参照するガード名と、実装されたガードの集合が一致すること

「1 時間を 1 回で進めた場合と 360 回で進めた場合が一致する」というテストは、実装が経過時間の累積ではなく絶対時刻で判定していることを保証する。これは Phase 2 でサーバが再起動したときの正しさに直結する。

**完了条件**: 冪等性と時間の飛びの性質テストが緑。遷移表の全行が到達可能。
---

### 段 D — 測る

---

#### PR 13 — 待ち時間の推定（ETA）

> **問い: ETA は 7.13 の v1 アルゴリズムのとおりか。単調性は保たれるか。**

**出典**: 全体プラン 7.13
**依存**: PR 7 ／ **見積**: 4h ／ **目安**: 350 行

**実装**

`packages/core/src/eta.ts`。7.13 の v1 アルゴリズムをそのまま実装する。

1. 人数 `p` が収まるテーブル集合 `T_p` を求める
2. 各卓の推定残り時間 `r_t` を求める。`FREE` / `TURNOVER` は 0、`HELD` はホールドの残り ＋ 平均滞在、`OCCUPIED` は `max(0, D − 経過時間)`（`D` は平均滞在時間、初期値 35 分）
3. `r_t` を昇順に並べ、自分より前にいて `T_p` を取り合う組数を `k` として、`k+1` 番目の `r_t` を目安とする
4. 表示は 5 分刻みの幅（`eta_display`）と「前に `k` 組」

`NEEDS_CHECK` と `OCCUPIED_UNKNOWN` の席をどう数えるかを決める必要がある。**7.13 に明記が無いので、この PR で決めて ADR に残す。** 推奨は「`NEEDS_CHECK` は空く可能性が高いので `OCCUPIED` の半分の残り時間として数え、`OCCUPIED_UNKNOWN` は `OCCUPIED` と同じに数える」。悲観側に寄せるという 7.13 の原則に従う。

**テスト**

- 空席があるときは「5 分未満」
- 前に k 組いるときに k+1 番目の席の残り時間が返ること
- **単調性**: 状態が変わらないまま時間が進むと、ETA は減るか同じ（増えない）
- **悲観性**: 返される目安が、幅の下限ではなく上限寄りであること
- 自分の人数が収まる席が 1 つも無いとき（定員超過）の扱い
- 5 分刻みの丸め（12 分 → 「10〜15 分」）
- シミュレータと突き合わせ、**予測と実績の差（MAE）を測る**。数字は PR 15 のレポートに載せる

最後の項目が重要で、ETA の精度を「良さそう」ではなく数字で言えるようにする。

**完了条件**: 7.13 の v1 が実装され、単調性の性質テストが緑。MAE が測れる。

---

#### PR 14 — 指標の収集と CLI

> **問い: 8.3 の指標がすべて出るか。CLI が使えるか。**

**出典**: 全体プラン 8.3、8.4
**依存**: PR 8、12、13 ／ **見積**: 6h ／ **目安**: 500 行

**実装**

- **指標の収集**（8.3 の全項目）
  - 待ち時間の平均・p90・最大を **人数別に**（2 名組と 4 名組の差を必ず出す。7.6 の設計判断の検証がこれに掛かっている）
  - 席の稼働率、ホールドで遊んだ時間の割合、ノーショーで失われた席時間
  - 「案内された席が塞がっていた」事故の件数と率
  - 放置・期限切れ・キャンセルの率
  - スタッフの介入回数（`NEEDS_CHECK` の解消）
  - ETA の予測誤差（PR 13）
- **自由席のベースライン**（8.1 の最終行）: 占有率の関数で空席探索時間をモデル化し、OpenSeat 無しの場合の着席までの時間を出す。**比較の相手がいないと「良くなった」と言えない**ため、レポートの説得力はここに掛かる
- **CLI**: `pnpm sim --scenario weekend-peak --policy default --runs 200 --seed 1 --out result.csv`
- **出力**: CSV（1 行 1 実行、列は指標）と、簡易 HTML レポート（分布のヒストグラム）
- **CI への組み込み**: 少ない回数（20 回程度）のファズを CI で走らせ、不変条件違反を検出する。200 回は手元で

**テスト**

- 指標の計算が、手で作った小さなシナリオで正しいこと（待ち時間 3 件の p90 など）
- CSV の列が 8.3 の項目を網羅していること
- 同じシードで同じ CSV が出ること
- ベースラインが妥当な範囲の値を返すこと（占有率 100% で探索時間が発散しないこと）

**完了条件**: 8.3 の指標がすべて出力され、CLI が動き、CI でファズが回る。

---

#### PR 15 — 方針比較レポートと ADR

> **問い: 既定値は数字で裏づけられたか。**

**出典**: 全体プラン 8.2、8.4、7.17、16.2 の #6・#7
**依存**: PR 14 ／ **見積**: 6h ／ **目安**: コード少、ドキュメント中心

**実装**

8.2 の 7 項目をすべて実行し、結果を読み、既定値を確定する。

| # | 比較する項目 | 振る値 |
|---|---|---|
| 1 | 割当 | 厳密 FIFO ／ 純 best fit ／ `fairness_override_min` = 5, 10, 15 |
| 2 | ホールド | `hold_min` = 5, 7, 10、延長あり／なし |
| 3 | ノーショー | `cancel` ／ `requeue_once` ／ `requeue_back` |
| 4 | 時間上限 | `off` ／ `soft` 45, 60 ／ `hard` 60 |
| 5 | 席の処理順 | 定員昇順のみ ／ 退席確認の新しさ優先を追加 |
| 6 | 確認要の割当 | `assign_needs_check` あり／なし |
| 7 | 片付け猶予 | `turnover_min` = 0, 1, 2 |

**成果物**

1. **`docs/260917_report_policy_comparison.md`** — 7 項目それぞれについて、表と 1 段落の結論。施設向け資料の付録になる（全体プラン 8.4）。各項目で「何を最適化したか」（平均待ち時間か、4 名組の p90 か、事故率か）を明記する。**トレードオフを隠さない。** 例えば `hold_min` を短くすると空席の遊びは減るが間に合わない人が増える、という関係を数字で示す
2. **ADR-0007〜0010**（9 章）
3. **全体プラン 7.16 の更新** — 既定値が変わった項目を反映し、根拠としてレポートを引く
4. **`docs/ROADMAP.md` の更新** — Phase 1 完了、Phase 2 へ

**この PR で気をつけること**

- **数字が既定値を支持しなかったときに、既定値のほうを変える。** レポートを既定値の追認に使わない。7.16 の値は「もっともらしい初期値」であって、結論ではない
- **シミュレーションの限界を明記する**（全体プラン 8.5）。人の行動（掲示に従う率、無断利用率）は観察でしか分からない。レポートは「ルールの相対比較」であり、絶対値は実証実験で確かめるものだと本文に書く
- 感度分析を入れる。到着率や滞在時間の初期値を ±30% 振ったときに結論が変わるなら、その結論は弱い

**完了条件**: レポートが存在し、ADR 4 本が書かれ、7.16 の既定値が数字に裏づけられている。
---

## 6. すべての PR に適用する横断ルール

各 PR の詳細には書かない。全部に効く。

### 6.1 進め方

1. **ブランチを切る。** `feat/p1-NN-短い説明`（例 `feat/p1-04-allocation-choose`）
2. **PR 本文に、対応する全体プランの節番号と「問い」を書く。** PR テンプレートの「影響範囲」で `packages/core` にチェックを入れる
3. **`pnpm verify` を通してから PR を出す。** 通っていないなら、通っていないと明示する
4. **マージはマージコミット**（squash しない）。`main` に直接 push しない

### 6.2 実装

- **`apply` の出口で必ず不変条件を検査する。** 破れていたら変更を破棄して拒否する。握り潰さない
- **遷移表にない遷移を起こさない。** 状態を直接書き換えるコードを書かない
- **新しいコマンドを足したら、ファズの生成器にも足す。** これを忘れると、そのコマンドはランダムテストの対象外になる
- **`packages/core` の純粋性を守る。** 時刻は引数、乱数は外から、I/O は無し。CI が検査する
- **運用パラメータを増やしたら 7.16 の表にも足す。** 表とコードの `Policy` が 1 対 1 であることは PR 1 のテストが守る

### 6.3 テスト

- **仕様の表は、1 行 1 テストで写す。** 7.6 のエッジケース表、7.8 の分岐表、7.16 のパラメータ表。人間が目で照合できる形にする
- **境界値を必ず書く。** 期限ちょうど、定員ちょうど、待ち 0 組、待ち上限
- **性質テストは、手書きのテストの代わりではなく追加。** 具体例は読んで分かり、性質は見落としを拾う。両方要る
- **テストの名前は日本語で、仕様の文言に寄せる。** 「4 名席が空いたとき、10 分未満の差なら 4 名組を優先する」のように書けば、テスト一覧が仕様書になる

### 6.4 ドキュメント

- **設計上の判断をしたら ADR を書く。** 1 ファイル 1 決定。迷ったら書く
- **全体プランと食い違いを見つけたら、実装を進める前に全体プランを直す。** 実装が仕様から静かに離れるのを防ぐ
- **本書（Phase 1 プラン）も更新する。** 見積もりが外れた、PR を分けた、順番を入れ替えた、はすべて記録に値する

---

## 7. 設計上の先決事項

実装を始める前に決めておくこと。迷いながら書くと後で揃わなくなる。

### 7.1 乱数を core に持ち込まない

`packages/core` は `Math.random()` を使えない（ADR-0004、CI で検査）。したがって **core の `apply` と `tick` は完全に決定的**にする。

確率的な要素はすべて呼び出し側に置く。「この人はノーショーする」「滞在は 32 分」という判断はシミュレータが下し、core には **コマンド、または何も起きないこと** として現れる。Phase 2 のサーバでは、それが実際の利用者の操作になる。

この設計により、シミュレータとサーバは同じ core を、同じ決定性で動かせる。

### 7.2 ID は外から渡す

ID の生成には乱数が要るため、core では作れない。`JOIN` コマンドは `ticketId` を含む。生成はシミュレータとサーバの責務。

表示コード（`A-23`）は例外で、状態から決定的に導けるため core が採番する。

### 7.3 失敗は例外ではなく `Result`

`apply` は `Result<Decision, Rejection>` を返す。理由はコードで表し、文言は Phase 2 の i18n に置く。純粋関数の失敗を型で表せ、呼び出し側が握り潰しにくい。

`assertInvariants` が投げる `InvariantError` だけは例外のままにする。これは「起きてはならないこと」で、呼び出し側が捕捉して変更を破棄する。通常の拒否（定員超過など）とは種類が違う。

### 7.4 状態は配列で持つ

`readonly Table[]` と `readonly Ticket[]`。規模（席 20〜100、有効チケット 200 程度）に対して線形走査で十分で、7.6 の擬似コードにも素直に対応する。引き当てはヘルパーに閉じ込め、後から `Map` に差し替えられるようにする。

### 7.5 イベントは Phase 1 では消費者がいない

`DomainEvent` は Phase 2 の永続化・配信・通知の入力になる。Phase 1 では発行するだけで、誰も読まない。それでも設計しておく。後から足すと、状態変化とイベントの対応に漏れが出る。

イベントは「起きたこと」を過去形で表す（`TicketCalled`、`TableFreed`）。コマンド（命令形）と混ぜない。

### 7.6 タイムゾーンは core に持ち込まない

core は UTC のミリ秒だけを扱う。運用時間帯の判定（7.14）に必要な「施設のローカル時刻」は、境界側で変換して core に渡す。core に `Intl` やタイムゾーンデータベースを持ち込まない（依存ゼロを守る）。

---

## 8. スケジュール

週 12〜15 時間を想定する。合計 77 時間なので、**5〜6 週**（週 15 時間なら 5.1 週、週 12 時間なら 6.4 週）。全体プランは 5 週としているが、正直に 6 週まで見る。

| 週 | PR | 時間 | 区切りで得られるもの |
|---|---|---|---|
| 1 | 1、2、3 | 11h | 仕様がコードになる。以降のレビュー基準が揃う |
| 2 | 4、5 | 12h | 割当が動き、受付ができる |
| 3 | 6、7、8 | 17h | **基本の循環が閉じ、シミュレータが回り始める** |
| 4 | 9、10、11 | 16h | 異常系と時間上限。実証実験で必要な振る舞いが揃う |
| 5 | 12、13、14 | 15h | 固めて、測れるようになる |
| 6 | 15 | 6h | 既定値が数字で決まる。Phase 1 完了 |

**3 週目が山場。** ここを越えるとシミュレータが以降の PR を自動で叩いてくれるので、進みが軽くなる。

**週の区切りで止まれる。** 各週の終わりで `main` は緑で意味のある状態なので、中断して Phase 0 の残り（現地観察、ドメイン取得）に戻ってもよい。

---

## 9. Phase 1 で決めること（ADR）

Phase 1 が終わるまでに、次の 4 つを決めて ADR に残す。いずれも **PR 15 のレポートを根拠にする。**

| ADR | 決めること | 現在の暫定値 | 全体プランの未決事項 |
|---|---|---|---|
| 0007 | 割当方式と `fairness_override_min` | ロス最小 ＋ 10 分 | — |
| 0008 | ホールド時間とノーショー方針 | `hold_min` = 7、`requeue_once` | 16.2 #7 |
| 0009 | 着席時間の上限 | `soft` 60、待ちがいる時のみ | 16.2 #6 |
| 0010 | 待ち時間の推定方式と、不確かな席の数え方 | 7.13 の v1 | — |

**暫定値を結論にしない。** 数字が支持しなければ変える。変えた場合は全体プラン 7.16 と 7.17 も更新する。

Phase 1 では決めない（据え置く）もの:

- 16.2 #10（5 名以上の分割登録）— v1 は拒否で確定済み。連結チケットは v2
- 16.2 #11（飛び込み着席）— 許可で確定済み。PR 9 で実装する
- 16.2 #4（LINE 通知）、#5（使用中カード）— 実証実験で決める
---

## 10. リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| **シミュレータまで辿り着かず、既定値の根拠が得られない** | 高 | PR 8 でシミュレータの骨格を中盤に置く（3.2 の判断 3）。日程が押したら PR 9・10・11 を Phase 2 へ送り、レポートは落とさない |
| 遷移表と実装が乖離する | 高 | PR 12 で網羅性を検査して閉じる。宣言されたすべての遷移が到達可能であることをテストにする |
| 7.6 のエッジケースを取りこぼす | 中 | 表の 9 行を 1 行 1 テストで写す。PR 4 の完了条件にする |
| `tick` が経過時間の累積で判定され、時間が飛ぶと壊れる | 中 | PR 12 で「1 時間を 1 回で進めた場合と 360 回で進めた場合が一致する」テストを書く。Phase 2 の再起動の正しさに直結する |
| 不変条件が緩すぎて、壊れた状態を通してしまう | 中 | 各不変条件に「破れる例」のテストを必ず書く（PR 3）。破れる例を作れないなら、それは型で保証されているということなので宣言から外す |
| シミュレータが core と別の実装を持ってしまう | 中 | CI の境界検査に「`sim` が `core` の `apply` / `tick` 以外で状態を変えない」検査を足すことを検討する。少なくともレビューで見る |
| 見積もりが外れて Phase 1 が延びる | 中 | 週の区切りで止まれる分解にしてある。4 章の「最短の打ち切り線」に従って落とす順を決めてある |
| ETA の精度が悪く、利用者の判断を誤らせる | 中 | PR 13 で MAE を測り、レポートに載せる。悪ければ「幅を広く取る」で対処し、統計モデルは v1.1 に送る |
| `soft` 上限や `assign_needs_check` の効果が数字に出ない | 低 | 出ないこと自体が結論。実証実験の検証項目（12 章）に送る。シミュレーションで人の行動は測れない（8.5） |
| 型が複雑になりすぎて読めなくなる | 低 | CLAUDE.md の 20 行制限と `complexity` の lint が効く。ブランド型を使わず `as` を避ける方針（PR 1）も、複雑さを抑える側に働く |

---

## 11. Phase 2 への引き継ぎ

Phase 1 の成果物が、Phase 2 で何になるかを先に書いておく。ここが繋がらないと分解を間違えている。

| Phase 1 の成果物 | Phase 2 での使われ方 |
|---|---|
| `apply` / `tick`（純粋関数） | `VenueActor` がコマンドを 1 件ずつ適用する。関数はそのまま |
| `Command` / `DomainEvent` | HTTP のルートがコマンドを組み立て、イベントを永続化して WebSocket に配信する |
| `Result<Decision, Rejection>` の `Rejection` | HTTP のステータスと利用者向け文言に写す。文言は `packages/shared` の i18n |
| 遷移表 | 変わらない。スタッフ操作を足すときも表に追記する |
| 不変条件 | 永続化のトランザクション内で検査する。破れたらロールバック |
| `Policy` | 施設ごとの設定として DB に持つ。`DEFAULT_POLICY` がプリセット「標準」になる |
| `chooseAssignments` | 変わらない。呼ぶきっかけ（退席、受付、設定変更）をサーバが作る |
| `resolveTableScan` | 座席 QR のルートが呼び、結果に応じて画面を返す。**分岐の判断は core のまま** |
| ETA | 利用者の画面とボードに出す |
| シミュレータ | 負荷試験の入力を生成する（9.12）。管理画面のシミュレーションタブ（v1.1）の土台 |
| 比較レポート | 施設への提案資料の付録（8.4）。運用設定のプリセット（10.3）の根拠 |

**Phase 2 の最初にやること**は、全体プラン 9.13 の 1 日スパイク（Hono ＋ SQLite ＋ Docker ＋ Railway）である。Phase 1 のコードには触れない。

**Phase 2 で新しく決める必要があるもの**（Phase 1 では決めない）: 権限表（CLAUDE.md 3.2(4)）、スキーマ（3.2(1)）、API 契約（3.2(5)）、冪等キーの設計、スナップショットとイベントログの形式。

---

## 12. 付録

### 12.1 Phase 1 完了時のディレクトリ構成（見込み）

```
packages/core/
  package.json          依存ゼロを維持
  src/
    time.ts             ✔ Phase 0
    decision.ts         ✔ Phase 0
    invariant.ts        ✔ Phase 0
    result.ts           PR 5（成功と拒否を型で表す）
    index.ts            ✔ Phase 0（Phase 1 で公開 API を追加）
    domain/
      ids.ts            PR 1
      table.ts          PR 1
      ticket.ts         PR 1
      state.ts          PR 1
      policy.ts         PR 1
    machine/
      transitions.ts    PR 2
      invariants.ts     PR 3
      apply.ts          PR 5（PR 6〜11 で育つ）
      tick.ts           PR 6（PR 10、11 で育ち、PR 12 で固まる）
      command.ts        PR 5（コマンドの語彙）
      rejection.ts      PR 5（拒否の語彙）
      events.ts         PR 5 以降
      transition.ts     PR 6（遷移表とガードの突き合わせ）
      deadlines.ts      PR 6（期限の計算。PR 10、11 で育つ）
      allocate.ts       PR 6（割当の適用）
      release.ts        PR 6（チケットの終わらせ方と席の解放）
      settle.ts         PR 7（席の期限・割当・検査の出口）
      guards.ts         PR 5 以降
    allocation/
      choose.ts         PR 4
      needs-check.ts    PR 10
    scan/
      resolve.ts        PR 9
    eta.ts              PR 13
    schedule.ts         PR 11
  sim/
    rng.ts              PR 8
    distributions.ts    PR 8
    scenario.ts         PR 8（PR 9〜11 で拡張）
    agent.ts            PR 8
    runner.ts           PR 8
    metrics.ts          PR 14
    baseline.ts         PR 14
    cli.ts              PR 14
```

### 12.2 PR の本文に書くこと

CLAUDE.md の PR テンプレートに加えて、Phase 1 では次を書く。

```markdown
## 問い
<この PR でレビュアーが答える 1 つの問い>

## 出典
全体プラン X.Y

## 仕様の表との対応
<7.6 のエッジケース表 9 行 → テスト 9 本、のような対応>
```

### 12.3 テストの名前の付け方

仕様の文言に寄せる。テスト一覧が仕様書として読める状態を目指す。

```ts
describe('割当（7.6）', () => {
  describe('公平性オーバーライド', () => {
    it('4 名席が空いたとき、待ち時間の差が 10 分未満なら 4 名組を優先する', ...);
    it('待ち時間の差が 10 分以上なら、2 名組を優先してロス 2 を受け入れる', ...);
    it('fairness_override_min = 0 なら厳密な先着順になる', ...);
    it('fairness_override_min = Infinity なら純粋な best fit になる', ...);
  });
});
```

### 12.4 参照する全体プランの節

| 節 | 内容 | 使う PR |
|---|---|---|
| 7.2 | 用語 | 1 |
| 7.3 / 7.4 | チケットとテーブルの状態機械 | 1、2 |
| 7.5 | 受付 | 5 |
| 7.6 | 割当アルゴリズム | 4 |
| 7.7 | 呼び出し・ホールド・遅刻 | 6 |
| 7.8 | 着席確認（座席 QR の 10 分岐） | 7、9 |
| 7.9 | キャンセル | 5 |
| 7.10 | 着席時間の上限 | 10 |
| 7.11 | 退席の申告率とゴースト占有 | 7、10 |
| 7.12 | 飛び込み利用 | 9 |
| 7.13 | 待ち時間の推定 | 13 |
| 7.14 | 運用時間帯とモード切替 | 11 |
| 7.16 | パラメータ一覧 | 1、15 |
| 8.1〜8.5 | シミュレーション | 8、14、15 |
| 9.4 | 実行モデル | 12 |
| 9.12 | テスト戦略 | 3、12 |
| 11.3 / 11.6 | Phase 1 の作業と工数 | 本書全体 |
| 16.2 | 未決事項 | 15 |
